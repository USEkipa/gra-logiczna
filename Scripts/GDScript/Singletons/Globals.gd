extends Node

var World: Dictionary = {GRAVITY = 20}

var Options: Dictionary = {
	isFullScreen = false,
	isMusic = false,
	volume = 5,
}

var PlayerStats: Dictionary = {
	SPEED = 100,
	JUMP_VELOCITY = -300,
	MAX_JUMPS = 1,
	maxBulletCount = 10,
	maxHealth = 100,
	bulletCount = 0,
	score = 0,
	health = 0,
}
